import { FundoVerdeDirective } from './fundo-verde.directive';

describe('FundoVerdeDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoVerdeDirective();
    expect(directive).toBeTruthy();
  });
});
